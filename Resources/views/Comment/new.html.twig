{% extends 'RudakBlogBundle::layout.html.twig' %}

{% block layout %}
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('rcmAdminIndex') }}">Administration</a>
        </li>
        <li>
            <a href="{{ path('admin_blog_comment') }}">
                Commentaires
            </a>
        </li>
        <li class="active">
            Creation du Commentaire
        </li>
    </ol>

    {{ form(form) }}
{% endblock %}
